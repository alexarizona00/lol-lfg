<div class="comment">
    {{#each this}}
    <div>
        <p class="commentText">
            {{commentContent}} - {{user.username}} {{format_date dateCreated}}
        </p>
    </div>
    {{/each}}
</div>