<div class="comment">
    {{#each this}}
    <div>
        <p class="commentText" id='#comment-posts'{{posts.id}} >
            {{comment.content}} - {{user.username}} {{format_date dateCreated}}
        </p>
    </div>
    {{/each}}
</div>